[project]
name = "retrieve-evaluation"
version = "0.1.0"
description = "Training and evaluation for retrieve framework"
requires-python = ">=3.10"
dependencies = [
    "retrieve",
    "torch>=2.4,<3",
    "polars>=1.0",
    "pyarrow>=15",
    "tqdm>=4.66",
    "click>=8.1",
    "huggingface-hub>=0.24",
    "loguru>=0.7",
    "numpy>=1.26",
    "pyyaml>=6.0",
    "torchvision>=0.19,<1",
    "wandb>=0.16",
    "sentence-transformers>=3.0",
    "einops>=0.7",
    "matplotlib>=3.8",
]

[project.scripts]
evaluate                = "retrieval.cli.evaluate:main"
run-evaluation          = "retrieval.cli.run_evaluation:main"
stage-results           = "retrieval.cli.stage_results:main"
upload-results          = "retrieval.cli.upload_results:main"
yambda                  = "datasets.yambda:main"
arxiv                   = "datasets.arxiv:main"
goodreads               = "datasets.goodreads:main"
upload-checkpoints      = "training.upload_checkpoints:main"
eval-fetch              = "datasets.hf_io:fetch_cli"
eval-publish            = "datasets.hf_io:publish_cli"
eval-publish-checkpoint = "datasets.hf_io:publish_checkpoint_cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["datasets", "retrieval", "training"]

[[tool.uv.index]]
name = "pytorch-cu128"
url = "https://download.pytorch.org/whl/cu128"
explicit = true

[tool.uv.sources]
retrieve = { workspace = true }
torch = { index = "pytorch-cu128" }
torchvision = { index = "pytorch-cu128" }

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "W", "F", "I", "UP"]

[tool.ruff.lint.isort]
known-first-party = ["datasets", "training", "retrieval", "retrieve"]
