"""What is on disk: the layout contract (``layout``), the Hub registry (``hub``), the shared
numerics (``common``, ``timesplit``, ``constants``) and, one level down, the per-dataset ETL
scripts (``etl/``) behind the ``eval-data`` console script (``cli``)."""
