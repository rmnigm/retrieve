data_dir: data/goodreads-work-id
output: results/voyager/goodreads-d128.json
split: test
device: cuda                   # for SASRec query encoding; the HNSW itself is CPU
ks: [100, 200, 400]
seed: 0
encode:
  batch_size: 512
  num_workers: 8
  max_seq_length: 200
algo_params:
  voyager_hnsw:
    - {m: 32, ef_construction: 200, ef_query: 1000, num_threads: 16, seed: 0}
checkpoint: data/goodreads-work-id/checkpoints/gsasrec-d128-drop0.5-id/best_model.pt
