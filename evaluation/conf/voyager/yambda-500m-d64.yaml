data_dir: data/yambda-500m
output: results/voyager/yambda-500m-d64.json
split: test
device: cuda                   # for SASRec query encoding; the HNSW itself is CPU
ks: [100, 200, 400]
seed: 0
encode:
  batch_size: 512
  num_workers: 8
  max_seq_length: 200
algo_params:
  voyager_hnsw:
    - {m: 32, ef_construction: 200, ef_query: 1000, num_threads: 16, seed: 0}
checkpoint: data/yambda-500m/checkpoints/gsasrec-d64-drop0.5/best_model.pt
